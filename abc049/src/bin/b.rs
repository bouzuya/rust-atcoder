use proconio::input;

fn main() {
    input! {
        h: usize,
        _w: usize,
        c: [String; h],
    };
    for c_i in c {
        println!("{}", c_i);
        println!("{}", c_i);
    }
}
